import 'package:flutter/material.dart';

class Tasks extends StatelessWidget {
  static const routeName = '/tasks';

  const Tasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('asgfdgasfdgfsgad'),
    );
  }
}
